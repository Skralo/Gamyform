PORT=3000
APP_ORIGIN=http://localhost:3000
# Run npm run setup to create a local owner password hash.
OWNER_PASSWORD_HASH=
# Leave empty to use durable local embedded PostgreSQL under ./data.
DATABASE_URL=
DATA_DIR=./data
# Optional AI draft integration (server only). Both values are required.
AI_API_KEY=
AI_MODEL=
